import { DataShowDirective } from './data-show.directive';

describe('DataShowDirective', () => {
  it('should create an instance', () => {
    const directive = new DataShowDirective();
    expect(directive).toBeTruthy();
  });
});
